{
  "name": "sfdc-marketing-chatbot",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "cd \"frontend\" && npm install && npm run build",
    "dev": "npm install && npm run build && nodemon index.js",
    "start": "npm install && npm run build && node index.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
